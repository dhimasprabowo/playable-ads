{
	"dependencies": {
		"cannon-es": "^0.20.0",
		"three": "^0.172.0",
		"threejs-fbxloader": "^0.0.1"
	},
	"devDependencies": {
		"vite": "^6.0.10",
		"vite-plugin-wasm": "^3.4.1"
	},
	"scripts": {
		"dev": "vite",
		"serve": "vue-cli-service serve",
		"build": "vue-cli-service build",
		"lint": "vue-cli-service lint",
		"predeploy": "npm run build", // Build the app before deploying
		"deploy": "gh-pages -d dist" // Deploy the contents of the 'dist' folder to the gh-pages branch
	}
}